/**
 * half your age + 7
 * @param {number} age age of person to check against.
 * @return {number} appropriate dating age.
 */
export const minDateableAge = () => {
}

/**
 * Calculate if you have a fever 
 * @param {number} temperature temperature to check.
 * @return {boolean} true if the user has a fever.
 */
export const hasFever = () => {
}

/**
 * Given a width on a 16:9 tv, calculate the height
 * @param {number} width width of TV.
 * @return {number} height of TV.
 */
export const calcTVHeight = () => {
}

/**
 * two people, could they date (deal with ages either way round)
 * @param {number} age1 age of person 1.
 * @param {number} age2 age of person 2.
 * @return {boolean} true if the people should be dating
 */
export const couldDate = () => {
}

/**
 * Inputs and outputs should be an object of colors, darkenAmount - don't let the amount drop below 0
 * @param {object} colours {r, g, b} values.
 * @param {number} darkenAmount The amount to darken the r,g,b values.
 * @return {number[]} array of darkend colours
 */
export const colorDarken = () => {
    return { red: -1, green: -1, blue: -1 }; // change this line and add others if needed
}
